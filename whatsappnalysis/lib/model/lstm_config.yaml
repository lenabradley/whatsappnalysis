# LSTM Model configuration
sequence_length: 100  # 50
num_layers: 3  # 3
num_units: 700 # 700
dropout_fraction: 0.2 # 0.2
activation: 'softmax'
loss: 'categorical_crossentropy'
optimizer: 'adam'
epochs: 50 # 10
fraction_to_train_on: 1